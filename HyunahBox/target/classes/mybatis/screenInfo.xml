<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="screenInfo">

	<select id="list" resultType="ScreenInfo">
		SELECT * FROM screenInfo WHERE theater_code=#{theaterCode}
	</select>
	
	<insert id="add">
		INSERT INTO screenInfo VALUES(SCREEN_CODE_SEQ.nextval, #{theaterCode}, #{screenNum}, #{sitTotal}, #{sitRow}, #{sitCol})
	</insert>
	
	<select id="getItem" resultType="ScreenInfo">
		SELECT * FROM screenInfo WHERE theater_code=#{theaterCode} AND screen_code=#{screenCode}
	</select>
	
	<update id="update">
		UPDATE screenInfo
		SET sit_total=#{sitTotal}, sit_row=#{sitRow}, sit_col=#{sitCol}, screen_num=#{screenNum}
		WHERE screen_code=#{screenCode}
	</update>
	
	<delete id="delete">
		DELETE FROM screenInfo WHERE theater_code=#{theaterCode} AND screen_code=#{screenCode}
	</delete>
	
	<select id="screenList" resultType="Integer">
		SELECT screen_code FROM screenInfo WHERE theater_code=#{theaterCode}
	</select>
 	
 </mapper>